proceed-((!nextBlockOccupied) && (!nextBlockBroken) && (!nextBlockClosed) && (!upcomingBlockOccupied) && (!upcomingBlockBroken) && (!upcomingBlockClosed))
switch-((!switchBlockOccupied) && (!destinationBlockOccupied) && (!switchBroken))
maintenance-((!previousBlockOccupied) && (!closeBlockBlockOccupied))
crossing-((!crossingBlockOccupied) && (!crossingBroken) && (crossingActivated))
crossingactivate-((previousBlockOccupied || crossingBlockOccupied))
monitor-((!nextBlockOccupied) && (!upcomingBlockOccupied))